# macOS
.DS_Store

# node dependencies (install with: npm install)
node_modules/

# vite production build output (regenerable via: make build)
dist/

# exported agent session transcripts (contain local absolute paths)
stats/
